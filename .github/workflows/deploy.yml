# GitHub Actions workflow disabled - using local build + push workflow only
# To re-enable, uncomment the sections below
#
# NOTE: This file is intentionally disabled to prevent automatic builds on GitHub.
# All deployments are managed via the local script: ./scripts/deploy.sh

# name: Build Images (Dev Branch Only)
# 
# on:
#   # No triggers - workflow disabled
#   workflow_dispatch: # Manual trigger only if needed for emergency
# 
# jobs:
#   disabled:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Workflow Disabled
#         run: |
#           echo "❌ GitHub Actions workflow is disabled"
#           echo "💡 Use local build + push workflow instead:"
#           echo "   ./scripts/deploy.sh build-and-push"